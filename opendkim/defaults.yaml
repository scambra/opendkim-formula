# -*- coding: utf-8 -*-
# vim: ft=yaml
opendkim:
  pkg: opendkim
  genkey_pkg: opendkim-tools
  config: '/etc/opendkim.conf'
  conf:
    UserID: "opendkim:opendkim"
    KeyTable: "/etc/opendkim/KeyTable"
    SigningTable: "/etc/opendkim/SigningTable"
  service:
    name: opendkim
